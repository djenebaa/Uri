version: '3'

services:
  web:
    container_name: Uri_backend
    build: .
    command: >
      sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    env_file:
    - .env
    ports:
      - "8218:8000"
